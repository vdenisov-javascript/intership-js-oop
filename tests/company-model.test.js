const assert = require('assert');


describe('Testing "company-model" from "Javacript-OOP"', () => {

  const companyModel = require('../tasks/company-model');

  it('I can`t imagine test for it', () => {
    const createdTable = {};
    const myResults = { reason: 'because all data will be generated randomly' };

    assert.deepEqual(createdTable, myResults);
  });

});
